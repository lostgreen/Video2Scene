# SceneActBench Milestone 0 State

- Goal: establish a pinned, read-only SceneActBench execution/evaluation boundary without
  changing Video2Scene GT generation.
- Current evidence: KML checkout `codex/sceneact-harness-foundation` at Video2Scene commit
  `7f932ad`; SceneActBench submodule at `5b01037454c2ef96c4dea4006b927d27da9d5447`.
- Last checks on KML: Ruff check and format check passed; mypy passed for 33 source files;
  pytest passed 21 tests; `make sceneact-doctor` returned `passed: true` with Blender 4.5.12
  LTS, MIT license, MCP, NumPy, SciPy, and `metrics_t6.py` present.
- Environment: reused `/home/xuboshen/Anaconda/envs/video2scene`; no new Conda environment was
  created. SceneAct paths and `DISABLE_TELEMETRY=true` are in the KML-only `.env.local`.
- Install artifact: `/home/xuboshen/zgw/log/video2scene_sceneact_install_20260803.log` completed
  successfully; `pip check` reported no broken requirements.
- Data state: no SceneActBench dataset or model artifact has been downloaded. The configured
  external root is `/m2v_intern/xuboshen/zgw/Video2Scene/external_data/sceneactbench`.
- Current risk: the `/m2v_intern` project root previously returned a quota error during another
  data-generation probe. Check a bounded write and the selected official sample size before any
  download.
- Stale evidence: earlier Data Collection MVP render logs do not establish SceneActBench scorer
  compatibility and must not be used for the next gate.

Next actions:

1. Inspect upstream task files and Hugging Face metadata to select one Dynamic scene ID.
2. Verify the external root can hold that scene, then fetch only its required files with license
   metadata.
3. Document the pinned `evaluate_t6` input/output contract and reproduce an oracle score before
   implementing local SceneAct package export.
